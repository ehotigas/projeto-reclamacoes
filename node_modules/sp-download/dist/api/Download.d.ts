import { IAuthOptions } from 'node-sp-auth';
import * as request from 'request';
import { IDownloadOptions } from '../interface/IDownload';
export declare class Download {
    private spr;
    private context;
    private agent;
    private logger;
    constructor(context: IAuthOptions, options?: IDownloadOptions);
    downloadFile: (spFileAbsolutePath: string, saveTo?: string) => Promise<string>;
    downloadFileFromSite: (siteUrl: string, spRelativeFilePath: string, saveTo?: string) => Promise<string>;
    downloadFileAsStream: (siteUrl: string, spRelativeFilePath: string) => Promise<request.Request>;
    private getWebByAnyChildUrl;
    private initContext;
    private promptForCreds;
    private getSaveFilePath;
}
export { IDownloadOptions } from '../interface/IDownload';
